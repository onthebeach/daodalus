test:
  cathouse:
    host: localhost
    database: daodalus
    pool_size: 5
    timeout: 5
    replicate_set_name: daodalus_test
    servers:
     - { host: localhost, port: 27017 }
  doghouse:
    host: localhost
    database: daodalus_2
    pool_size: 5
    timeout: 5
    replicate_set_name: daodalus_test
    servers:
     - { host: localhost, port: 27017 }
